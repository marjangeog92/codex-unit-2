## Level 05 — Select, Optgroup & Options

Objective: Add a `<select>` element with multiple `<option>`s and at least one `<optgroup>`.

Tasks:

- Create a `<select>` with two or more `<option>` elements.
- Group related options with `<optgroup label="...">`.

Hints:

- Provide useful option text and values for clarity.
