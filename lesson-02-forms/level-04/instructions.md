## Level 04 — Input Types

Objective: Practice using appropriate input `type` attributes: `email`, `tel`, `password`, `number`, `range`, `file`.

Tasks:

- Add inputs with the listed types inside the form.
- For `range`, include `min` and `max` attributes.

Hints:

- Use `input[type="file"]` for file uploads (no server needed for this exercise).
