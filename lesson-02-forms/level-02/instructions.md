## Level 02 — Name & Email Inputs

Objective: Add name and email inputs to the form. Defer full labeling until Level 7.

Tasks:

- Add a name input (text).
- Add an email input using `<input type="email">`.

Hints:

- The browser will validate `type="email"` for basic format checks.
