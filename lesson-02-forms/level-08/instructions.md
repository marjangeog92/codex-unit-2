## Level 08 — Grouping Controls

Objective: Use `<fieldset>` and `<legend>` to group related controls for improved semantics.

Tasks:

- Wrap related inputs in a `<fieldset>` and add a `<legend>` describing the group.

Hints:

- Fieldsets are especially useful for grouped radios or related address fields.
