## Level 03 — Message Field & Submit

Objective: Add a `<textarea>` for a message and ensure a submit control exists.

Tasks:

- Add `<textarea name="message"></textarea>` inside the form.
- Ensure a submit button is present.

Hints:

- Textareas do not use `value` attributes; include placeholder text if helpful.
