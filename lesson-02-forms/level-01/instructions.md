## Level 01 — Intro & Boilerplate

Objective: Add a basic `<form>` element containing at least one `<input>` and a submit `<button>`.

Tasks:

- Add a `<form>` element to the page.
- Inside the form, add a single text input (e.g., `<input type="text">`).
- Add a submit control (a `<button type="submit">` or `<input type="submit">`).

Hints:

- Use `action="#"` for the form while working locally.
- Keep elements semantic — avoid using `div` in place of form controls.

Do NOT paste full solutions here; use the TODOs in `index.html`.
